## FRONTEND

#include <iostream>

using namespace std;

string $funcname$(string s) {
    // Write your code here

    return "CodinLab"
}

int main() {
    std::string input;

    std::cout << "Bir string girin: ";
    std::getline(std::cin, input); 

    std::cout << std::string << isPalindrome(input) << std::endl;

    return 0;
}

## DOCKER


#include <iostream>

using namespace std;

$usercode$

int main(){
    $checks$

    std::cout << "$success$" << std::endl;
}

## CHECK

string result$rnd$ = $funcname$($input$);
if (result$rnd$ != $output$){
    std::cout << "Test Failed: Expected $out$, but got" << result$rnd$ << std::endl;

    exit($rnd$);
}