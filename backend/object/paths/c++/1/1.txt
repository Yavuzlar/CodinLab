## FRONTEND

using namespace std;

string $funcname$() {
    // Write your code here

    return "x"
}

int main() {
    std::string userInput;
    
    // Kullanıcıdan girdi almak için
    std::cout << "Bir değer girin: ";
    std::getline(std::cin, userInput);

    // funcname fonksiyonunu çağırıp sonucu alıyoruz
    std::string result = funcname(userInput);

    // Sonucu ekrana yazdırıyoruz
    std::cout << "Fonksiyondan dönen sonuç: " << result << std::endl;

    return 0;
}

## DOCKER

#include <iostream>
#include <string>

$usercode$

int main(){
    $checks$

    std::cout << "$success$" << std::endl;
    return 0;
}

## CHECK

std::string result = $funcname$($input$);
if (result != $output$){
    std::cout << "Test Failed: Expected $out$, but got" << result << std::endl;
}