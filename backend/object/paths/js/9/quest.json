{
    "id": 9,
    "languages": [
        {
            "lang": "en",
            "title": "Adding Two Numbers in JavaScript",
            "description": "Learn how to create a JavaScript function that adds two numbers and returns the result.",
            "content": "Write a JavaScript function named `addNumbers` that takes two parameters `a` and `b`, adds them together, and returns the result. Call the function with example values and log the output to the console.",
            "note": "1. Functions in JavaScript are created using the `function` keyword.\n2. A function can take parameters, which are used as variables inside the function. For example:\n   ```javascript\n   function add(a, b) {\n       return a + b; // Returns the sum of a and b\n   }\n   ```\n3. To call the function and see the result, use `console.log`. Example:\n   ```javascript\n   console.log(add(5, 10)); // Outputs: 15\n   ```\n4. The `return` statement ends the function and returns a value. If no `return` statement is used, the function returns `undefined`."
        },
        {
            "lang": "tr",
            "title": "JavaScript'te İki Sayıyı Toplama",
            "description": "İki sayıyı toplayan ve sonucu döndüren bir JavaScript fonksiyonunun nasıl oluşturulacağını öğrenin.",
            "content": "İki parametre `a` ve `b` alan, bu sayıları toplayıp sonucu döndüren `addNumbers` adlı bir JavaScript fonksiyonu yazın. Fonksiyonu örnek değerlerle çağırın ve sonucu konsola yazdırın.",
            "note": "1. JavaScript'te fonksiyonlar `function` anahtar kelimesi ile oluşturulur.\n2. Bir fonksiyon parametre alabilir ve bu parametreler, fonksiyon içinde değişken olarak kullanılır. Örneğin:\n   ```javascript\n   function topla(a, b) {\n       return a + b; // a ve b'nin toplamını döner\n   }\n   ```\n3. Fonksiyonu çağırmak ve sonucu görmek için `console.log` kullanın. Örnek:\n   ```javascript\n   console.log(topla(5, 10)); // Çıktı: 15\n   ```\n4. `return` ifadesi, fonksiyonu sonlandırır ve bir değer döndürür. Eğer `return` kullanılmazsa, fonksiyon `undefined` döner."
        }
    ],
    "quest": {
        "difficulty": 1,
        "funcName": "sumTwoNumbers",
        "tests": [
            {
                "input": [
                    2,
                    6
                ],
                "output": [
                    8
                ]
            },
            {
                "input": [
                    5,
                    9
                ],
                "output": [
                    14
                ]
            }
        ],
        "codeTemplates": [
            {
                "programmingID": 4,
                "templatePath": "object/paths/js/9/template.txt"
            }
        ]
    }
}